#requirements-dockers.txt
fastapi
uvicorn
pydantic
numpy
pandas
scikit-learn
joblib
mlflow
dagshub
lightgbm
xgboost
category-encoders
scipy
boto3
# Needed for your regex and date logic in data_clean_utils
python-dateutil
streamlit
plotly
wordcloud
requests
ddgs

# EstateMind Copilot API (src.api.main) runtime dependencies
python-dotenv
# Resend HTTPS email API (Phase 28.2, password reset on Railway —
# outbound SMTP ports are blocked there)
resend
# Claude API transport (src/llm/anthropic_client.py) when LLM_PROVIDER=claude
anthropic>=0.40.0
rank-bm25
fastmcp
# Report PDF export + WhatsApp document delivery (Phase 16.2) —
# headless Chromium renderer for /report/pdf and /report/share
playwright