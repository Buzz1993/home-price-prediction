// Placeholder dashboard page. The full dashboard workspace is built in Phase 4;
// this exists so the Phase 2 layout shell can render and be tested.
export default function DashboardPage() {
  return (
    <div className="space-y-1">
      <h1 className="font-heading text-2xl font-semibold">Dashboard</h1>
      <p className="text-sm text-muted-foreground">
        Your EstateMind workspace will appear here.
      </p>
    </div>
  );
}
